name Debug Folder Structure

on
  push
    branches [ main ]
  workflow_dispatch

jobs
  debug
    runs-on ubuntu-latest
    steps
      - name Checkout repository
        uses actionscheckout@v4

      - name Show working directory and files
        run 
          echo Current working directory
          pwd
          echo -----------------------------------
          echo Files and folders in this repo
          ls -R
